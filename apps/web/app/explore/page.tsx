"use client";
import { Avatar, SocialShell } from "@/components/site-chrome";import { FeedList } from "@/components/social/feed-list";import { api } from "@/lib/api";import type{PublicUserProfile}from"@aura/shared";import Link from"next/link";import{useEffect,useState}from"react";
export default function ExplorePage(){const[q,setQ]=useState("");const[users,setUsers]=useState<PublicUserProfile[]>([]);useEffect(()=>{const t=setTimeout(()=>{if(q.trim())api<{data:PublicUserProfile[]}>(`/v1/users/search?q=${encodeURIComponent(q)}`).then(r=>setUsers(r.data)).catch(()=>setUsers([]));else setUsers([]);},250);return()=>clearTimeout(t)},[q]);return <SocialShell><header className="sticky top-16 z-20 border-b border-white/[.07] bg-[#070708]/85 p-4 backdrop-blur-xl lg:top-0"><h1 className="mb-4 font-medium">Explore</h1><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search people" aria-label="Search people" className="h-11 w-full rounded-full border border-white/[.08] bg-white/[.04] px-5 text-sm outline-none focus:border-violet-400/50"/>{users.length>0&&<div className="mt-3 overflow-hidden rounded-2xl border border-white/[.08] bg-zinc-950">{users.map(user=><Link href={`/profile/${user.username}`} key={user.id} className="flex items-center gap-3 border-b border-white/[.06] p-3 last:border-0 hover:bg-white/[.04]"><Avatar user={user} size="sm"/><span><span className="block text-sm">{user.displayName}</span><span className="text-xs text-zinc-600">@{user.username}</span></span></Link>)}</div>}</header><div className="px-5 py-4 text-xs font-medium uppercase tracking-[.18em] text-zinc-600">Recent across AURA</div><FeedList endpoint="/v1/explore"/></SocialShell>}
